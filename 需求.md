这是一份专门为 **Cursor (或 GitHub Copilot Workspace)** 编写的完整开发需求文档（PRD）。你可以直接将以下内容整体复制，保存为项目根目录下的 `PRD.md`，然后配合 Cursor 的 `@PRD.md` 功能进行分步代码生成。

---

# 开发需求文档 (PRD)：Upwork 智能精准筛选与半自动投递系统

## 1. 项目概述 (Project Overview)

本系统旨在通过自动化手段，全天候监控 Upwork 的新发布任务，利用大模型（LLM）进行“事少钱多”策略过滤与技术匹配度评估，最终通过 Telegram 推送给用户。系统包含上下班打卡机制以控制推送，并配合一个轻量级 Chrome 插件，实现一键触达并自动填充定制化 Cover Letter 的半自动投递流。

### 核心技术栈

- **数据源：** Apify Scraper (`neatrat/upwork-job-scraper`)
- **后端服务：** Node.js (Express.js), 部署于 Hostinger
- **轻量数据存储：** SQLite 或 Local JSON File（适合 Hostinger 基础环境）
- **通知层：** Telegram Bot API
- **前端触达：** Chrome Extension (Manifest V3)
- **AI 层：** OpenAI / Gemini API (用于语义评估与文本生成)

---

## 2. 系统架构与工作流 (Architecture & Workflow)

```
[Apify 定时任务/Webhook]
       │ (新任务 JSON 数据)
       ▼
[Hostinger Node.js 后端]
       │ 1. 检查当前是否为“上班打卡”状态 (若下班则丢弃或挂起)
       │ 2. 硬性指标过滤 (钱多: 预算、已验证、客户消费额)
       │ 3. LLM 语义过滤 (事少: 评估风险点、生成 Cover Letter)
       │ 4. 存储任务快照到本地数据库
       ▼
[Telegram Bot 推送] ────(点击投递链接 带参数 ?ref=mybot&job_id=xxx)────> [用户浏览器]
                                                                        │ (唤起插件)
                                                                        ▼
                                                             [Chrome Extension]
                                                                │ 请求后端获取 Cover Letter
                                                                ▼
                                                             [自动填充到 Upwork 页面]

```

---

## 3. 详细功能模块设计 (Module Specifications)

### 模块 A：后端状态管理与打卡系统 (Duty Status Management)

- **需求描述：** 系统需要持久化记录当前的“上下班”状态。
- **存储结构 (SQLite/JSON)：**

```json
{
  "is_on_duty": true,
  "last_toggle_time": "2026-05-17T10:00:00Z"
}
```

- **Telegram 指令交互：**
- 用户发送 `/checkin`：修改 `is_on_duty` 为 `true`，Bot 回复：“上班打卡成功！正在为您监控神仙项目。”
- 用户发送 `/checkout`：修改 `is_on_duty` 为 `false`，Bot 回复：“下班打卡成功！已暂停消息推送，好好享受生活。”

### 模块 B：数据接收与硬性过滤引擎 (Data Ingestion & Hard Filtering)

- **接口：** POST `/api/webhook/upwork-jobs`（供 Apify Webhook 调用）
- **逻辑流程：**

1. 校验请求有效性。
2. 读取当前打卡状态，若 `is_on_duty == false`，直接结束流程。
3. **硬性指标过滤（可配置）：**

- `client.isPaymentVerified` 必须为 `true`。
- `client.rating` 必须 `≥ 4.5`（若有）。
- `client.totalSpent` 必须 `≥ $10,000`（排除零消费新手客户）。
- `proposalsCount` 必须 `< 15`（确保进去不是炮灰）。

4. 通过硬性过滤的任务，进入 **模块 C（LLM 评估）**。

### 模块 C：LLM 智能评估与提案生成 (AI Evaluation Layer)

- **输入：** 任务标题、描述、预算、技能标签。
- **Prompt 核心设定（作为环境变量或配置文件传入）：**

```text
你是一名资深全栈设计开发专家（8年经验，精通全栈开发、React、WordPress、Shopify 主题及应用扩展开发，同时精通 3D 产品建模与 KeyShot/Blender 高级渲染）。
请根据以下 Upwork 任务信息进行评估：
1. 评分系统：给出 1-10 的“事少钱多”匹配分。
2. “事少”评估：检查是否有红旗词汇（如无限修改、模糊画饼、极低预算要全能）。
3. “钱多”评估：客户是否有良好的支付历史和充足预算。
4. 生成 Cover Letter：如果匹配分 ≥ 8 分，结合任务痛点，以专业、地道、直击要害的口吻编写一篇 200 字以内的英文 Cover Letter（突出全栈+3D双重优势，拒绝AI套话）。

返回格式必须为 JSON：
{
  "score": 9.2,
  "reason_low_effort": "需求非常明确，附带Figma，无无限修改字眼",
  "reason_high_pay": "固定预算高，客户历史客单价大",
  "cover_letter_preview": "摘要...",
  "cover_letter_full": "完整版..."
}

```

- **持久化：** 将生成的任务详情、分数、`cover_letter_full` 保存至数据库，以 `job_id` 为主键，过期时间 7 天。

### 模块 D：Telegram 消息组装与推送 (Notification Engine)

- **接口：** 向 Telegram Bot 发送 `sendMessage`。
- **消息模板（MarkdownV2 格式）：**
  （参考用户原型设计，包含标题、预算、竞争度、AI 简评及按钮）
- **Inline Keyboards (内联按钮设置)：**
- 按钮 1：`🔗 直达 Upwork 投递页`
- URL 格式：`[https://www.upwork.com/ab/proposals/job/$](https://www.upwork.com/ab/proposals/job/$){job_id}/apply/?ref=mybot&job_id=${job_id}`

- 按钮 2：`📋 复制完整 AI 提案`
- 利用 TG 复制机制，或绑定 callback 触发文本单独发送。

### 模块 E：Chrome 自动填充插件 (The Chrome Extension)

包含两个核心文件，**纯原生开发，不依赖任何构建工具**。

#### 1. `manifest.json` (Manifest V3)

```json
{
  "manifest_version": 3,
  "name": "Upwork Copilot",
  "version": "1.0",
  "permissions": ["activeTab", "scripting"],
  "host_permissions": [
    "https://www.upwork.com/*",
    "http://你的Hostinger域名/*"
  ],
  "content_scripts": [
    {
      "matches": ["https://www.upwork.com/ab/proposals/job/*/apply/*"],
      "js": ["content.js"],
      "run_at": "document_end"
    }
  ]
}
```

#### 2. `content.js`

- **执行逻辑：**

1. 解析当前页面 URL，检查是否包含参数 `ref=mybot`。
2. 如果包含，提取 `job_id` 参数。
3. 发起 `fetch` 请求到 Hostinger 后端：`GET http://你的Hostinger域名/api/proposal?job_id=${job_id}`。
4. 拿到 JSON 中的 `cover_letter_full`。
5. **DOM 注入逻辑：**

- 设置定时器（如每 500ms 检查一次），等待 Upwork 页面的 Cover Letter 输入框（通常是 `textarea[id^="cover-letter"]` 或 `textarea.cl-textarea`）加载完成。
- 找到输入框后，将文本填入：`textarea.value = cover_letter_full`。
- **关键点：** 触发必要的网页事件（如 `input`、`change`、`blur`），确保 React/Vue 框架能监听到数据的改变，激活底部的 Submit 按钮。

```javascript
textarea.dispatchEvent(new Event("input", { bubbles: true }));
```

---

## 4. 后端 API 接口定义 (API Endpoints)

### 1. `POST /api/webhook/upwork-jobs`

- **描述：** 接收 Apify 抓取到的新任务。
- **安全：** 可选加入 Token 校验，防止恶意请求。

### 2. `GET /api/proposal`

- **描述：** 供 Chrome 插件调用的跨域接口。
- **参数：** `?job_id=xxx`
- **返回：**

````json
    {
      "success": true,
      "job_id": "xxx",
      "cover_letter": "The complete tailored cover letter text..."
    }
    ```
*   **注意：** 后端需开启 CORS 允许 `[https://www.upwork.com](https://www.upwork.com)` 的请求。

---

## 5. 给 Cursor 的分步开发指令 (Prompt Strategy for Cursor)

当你准备好开始写代码时，请分批向 Cursor 发送以下指令：

*   **步骤 1 (后端骨架)：**
    > "@PRD.md 请基于 Node.js Express 框架，初始化 Hostinger 后端项目结构。包含本地数据存储方案（SQLite 或 JSON 文件），实现 `/api/webhook/upwork-jobs` 接收端，并搭建打卡逻辑（`/checkin` 和 `/checkout` 状态切换）。"
*   **步骤 2 (Telegram 与 AI 路由)：**
    > "@PRD.md 请在后端集成 Telegram Bot SDK。编写模块 C 和 模块 D，实现硬性条件过滤、调用 LLM（使用官方 SDK 或 HTTP 请求）按照 PRD 中的 Prompt 进行评估，并格式化输出 Telegram 消息和内联按钮。"
*   **步骤 3 (Chrome 插件开发)：**
    > "@PRD.md 请在新建的 `extension/` 目录下编写满足 Manifest V3 规范的 Chrome 插件。实现检测 URL 参数、从后端异步获取 Cover Letter 并采用事件驱动的方式自动填充到 Upwork 的文本框中。"

````
